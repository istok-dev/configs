---
"@istok-dev/configs": minor
---

Добавлен skill `react-project`: конвенции React-приложения (`@istok-dev/web-ui`, формы через react-hook-form).

В `nest-project-structure` и `next-project-structure` добавлены секции tsconfig и ESLint; в Nest — также OpenAPI и генерация API-пакетов для клиентов.

В `prisma` запрещено менять уже применённые миграции; в `workspace` добавлена ссылка на `react-project`.
